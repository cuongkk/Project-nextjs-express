export const PAGINATION = {
  // Số item mỗi trang cho danh sách job của 1 công ty (backend quản lý công ty)
  COMPANY_JOB_PAGE_SIZE: 12,

  // Số item mỗi trang cho danh sách công ty
  COMPANY_LIST_PAGE_SIZE: 12,

  // Số item mỗi trang cho danh sách CV phía công ty
  COMPANY_CV_PAGE_SIZE: 6,

  // Số item mỗi trang cho danh sách CV phía user
  USER_CV_PAGE_SIZE: 6,

  // Số item mỗi trang cho kết quả tìm kiếm job
  SEARCH_JOB_PAGE_SIZE: 12,

  // Số item mỗi trang cho danh sách đơn ứng tuyển của user
  COMPANY_APPLICATION_PAGE_SIZE: 12,

  // Số item mỗi trang cho danh sách đơn ứng tuyển của company
  USER_APPLICATION_PAGE_SIZE: 6,

  // Số item mỗi trang cho danh sách công việc chung (trang chủ, tìm kiếm)
  JOB_LIST_PAGE_SIZE: 10,
};

// Thời gian hết hạn mặc định cho job (tính theo ngày)
export const JOB_EXPIRE_DAYS = 30;
